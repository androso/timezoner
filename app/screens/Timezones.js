import React, { useEffect } from "react";
import { useRouter } from "next/router";

export default function Timezones({ submittedForm }) {
	

	useEffect(() => {
		console.log(submittedForm);
	
	});
  
	return <div>TableTimezone</div>;
}
